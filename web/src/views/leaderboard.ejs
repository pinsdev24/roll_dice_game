<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GeekIT Dice Game</title>
    <!-- <link href="https://cdn.tailwindcss.com" rel="stylesheet" type="text/css"> -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">
    <nav class="bg-blue-600 text-white p-4">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/" class="text-2xl font-bold">GeekIT Dice Game</a>
            <div>
                <a href="/" class="mr-4 hover:underline">Home</a>
                <a href="/game" class="mr-4 hover:underline">Play</a>
                <a href="/leaderboard" class="hover:underline">Leaderboard</a>
            </div>
        </div>
    </nav>

    <main class="container mx-auto mt-8 p-4">
        <div class="max-w-2xl mx-auto">
            <h1 class="text-3xl font-bold mb-8 text-center">Leaderboard</h1>
            <div class="bg-white rounded-lg shadow-lg overflow-hidden">
                <table class="w-full">
                    <thead>
                        <tr class="bg-blue-600 text-white">
                            <th class="p-2">Rank</th>
                            <th class="p-2">Player</th>
                            <th class="p-2">Score</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(let i = 0; i < 10; i++) { %>
                            <tr class="<%= i % 2 === 0 ? 'bg-gray-100' : 'bg-white' %> hover:bg-gray-200 transition duration-300">
                                <td class="p-2 text-center"><%= i + 1 %></td>
                                <td class="p-2">Player <%= i + 1 %></td>
                                <td class="p-2 text-center"><%= Math.floor(Math.random() * 1000) %></td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </main>

    <!-- <footer class="bg-blue-600 text-white p-4 mt-8">
        <div class="container mx-auto text-center">
            &copy; 2024 GeekIT Dice Game. All rights reserved.
        </div>
    </footer> -->

    <script src="/js/main.js" type="text/javascript"></script>
</body>
</html>